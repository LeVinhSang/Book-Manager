<html lang="en">
    <head>
        <title>Book-Manager</title>
        <meta charset="UTF-8">
    </head>

    <body>
        <header>
            <h2>Book-Manager</h2>
        </header>

        <nav>
            <form>
                <a href="/">Home</a> |
                <a href="/books">Books</a> |
                <a href="#">Publishers</a>
            </form>

            <form method="get" action="/search-basic">
                <input name="keyword" placeholder="Search">
                <button type="submit">Search</button>
            </form>

            <form method="post" action="/edit/{{book.getId()}}">
                <input type="hidden" name="_method" value="PUT"/>
                <input name="title" value="{{book.getTitle()}}" placeholder="Title">
                <input name="author" value="{{book.getAuthor()}} " placeholder="Author">
                <label for="publisher">Publisher</label>
                <select id="publisher" name="publisher_id">
                    <option value="{{book.getPublisher().getId()}}">{{book.getPublisher().getId()}}</option>
                    {% for publisher in publishers %}
                    <option value="{{publisher.getId()}}">{{publisher.getId()}}</option>
                    {% endfor %}
                </select>
                <input name="price" value="{{book.getPrice()}}" placeholder="price">
                <button type="submit">Edit</button>
            </form>

            <form>
                <a href="/create">Create Book</a>
            </form>
        </nav>
    </body>
</html>